# ADR-075 — Reference Exposure Aggregation

Status: Accepted

Exposure aggregation normalizes explicit position snapshots into reference gross notional, explicit capital at risk, ticker totals, and optional sector totals. Unsupported or incomplete data is surfaced as partial, not treated as zero.
