# ADR-103: Selected Proposal Identity

Status: Accepted

Selected proposal identity is a deterministic SHA-256 over normalized reference-selection inputs. It is the future binding target for production approval and execution authorization.

It excludes production approval, execution authorization, promotion result, final decision fingerprint, random values, and hidden current time.
