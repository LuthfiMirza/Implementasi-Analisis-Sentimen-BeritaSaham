One Small Controlled Strategy Hypothesis V2
===========================================

- artifact = one_small_controlled_strategy_hypothesis_v2
- generated_at = 2026-04-23T15:11:26Z
- previous_hypothesis_reference = output/one_small_controlled_strategy_hypothesis.json
- selected_next_lever = entry_signal_persistence_minimum_confirmation_days
- hypothesis_statement = Adding a minimum confirmation-days requirement before a Layer 2 entry signal is treated as valid, while keeping the rest of the frozen stack unchanged, can improve out-of-sample average return per trade above zero without letting out-of-sample max drawdown remain above 35% under the same walk-forward and realism-cost policy.
- why_this_hypothesis_now = Hipotesis v1 untuk `absolute_momentum_floor_on_return_20d` sudah formal gagal dalam range yang dikunci, audit OOS versus holdout menunjukkan `comparable_regime_share`, dan diagnosis yang ada tetap menunjuk kelemahan utama pada kualitas edge entry, sehingga lever berikutnya tetap harus berada di Layer 2 entry quality; minimum confirmation days lebih defensible daripada membuka kembali momentum floor yang sudah ditolak.

- what_changes:
  - component = Layer 2 entry eligibility
  - single_variable = entry_signal_persistence_minimum_confirmation_days
  - current_behavior = no explicit minimum confirmation-days requirement is applied before a qualifying Layer 2 entry condition is treated as valid
  - proposed_hypothesis_change = add a minimum confirmation-days requirement to the Layer 2 entry eligibility check while leaving the rest of the stack unchanged
  - change_scope = one variable only

- what_stays_frozen:
  - Layer 1 regime rule stays the current active policy: `ihsg_ema50 > ihsg_ema200 AND ihsg_ema50 > prev_ihsg_ema50`.
  - Layer 2 family stays the same except for the selected confirmation-days lever.
  - The `return_20d` momentum-floor range is not reopened.
  - Layer 3 stays optional off-by-default.
  - Layer 5 ATR multiplier stays 2.5.
  - Layer 5 trailing-stop logic stays unchanged.
  - Time stop stays fixed at 12 days.
  - Position sizing stays 2% risk per trade and 10% max position cap.
  - Universe stays unchanged.
  - Walk-forward split stays unchanged.
  - Allow-overlap policy stays unchanged.
  - Realism cost policy stays unchanged.
  - Corporate-action ATR temporary policy stays unchanged.
  - Governance blocked state stays unchanged.
  - Retest remains blocked.
  - Paper trading remains blocked.

- minimum_pass_criteria:
  - metric_basis = same walk-forward evaluation with the same realism-cost policy already documented
  - oos_avg_return_per_trade_gt = 0.0
  - oos_max_drawdown_pct_lt = 35.0
  - holdout_avg_return_per_trade_floor = -0.1
  - trade_count_floor = 350
  - no_worse_than_current_holdout = true

- failure_conditions:
  - Fail if out-of-sample average return per trade remains less than or equal to 0.0.
  - Fail if out-of-sample max drawdown remains greater than or equal to 35.0%.
  - Fail if holdout average return per trade stays below -0.1.
  - Fail if trade count falls below 350 in the evaluated window set.
  - Fail if holdout performance becomes worse than the current frozen stack after costs.
  - Fail if out-of-sample average return per trade improves but holdout average return per trade remains materially weak below -0.1, because that would indicate only an OOS-side improvement without acceptable holdout robustness.

- why_this_is_not_a_full_rebuild = Ini tetap bukan full rebuild karena fondasi strategi, evaluasi, dan realism-cost policy yang sama tetap dipakai apa adanya. Hipotesis v2 ini hanya mengisolasi satu refinement kecil pada kualitas entry Layer 2, yaitu persistence minimum confirmation days, tanpa mengubah Layer 1, sizing, exit, universe, walk-forward split, atau governance state.
- next_action = prepare_single_variable_experiment_spec_v2_without_running_it_yet
- retest_status = blocked
- paper_trading_status = blocked
